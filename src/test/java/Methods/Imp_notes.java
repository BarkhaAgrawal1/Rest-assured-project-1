package Methods;

public class Imp_notes {

    // click right on the project then conver in to test ng will create testng. xml
    // then right click on the testng.xml click run modification it will create testoutput package
    // in testout put package open in browsers then emailable report will generate.
}
